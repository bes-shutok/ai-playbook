# Staging docs r1-r5 (phase3 reconciliation run) fail --hard on per-finding canonical_pattern; schema-retrofit or validator-contract decision needed

Evidence: validate_review_staging.py --hard rejects docs/reviews/2026-09-18-execute-plan-phase3-process-reconciliation-trigger-and-archive-safety-code-review-r{1..5}.md (+sidecars) on the current-v1 extended schema: every version-1 finding row needs a canonical_pattern field, plus witness-ledger row shape. The five docs were written in the compact legacy shape during the 2026-09-18 execution run; structural completions (## Metadata, ### Panel, freshness lines, extensions review_mode/risk_signals/prior_findings_filter, sidecar overflow/soften_watchlist/artifact_slug/round/panel_mode/selection_reason/source_digest/counts) were retrofitted in the 2026-09-19 done run, leaving only the canonical_pattern-class errors. Adding 24 post-hoc canonical patterns would be reconstruction, not completion.

Direction: either (a) a one-off retrofit script that derives canonical_pattern from each finding's id/title/files (with human review), or (b) a documented compact-shape profile in the validator for pre-2026-09-19 docs (freshness-gated, read-only), so historical staging stops failing --hard. Also decide whether the done Step 2.64 gate should hard-fail on historical docs at all (its stub-prevention purpose is served; content is complete).
