# Deferred plans

Plans parked by the efficiency/token/simplicity triage (2026-09-11). Files are byte-identical to their certified state — nothing was edited, so their review digests stay valid if a plan is ever revived. Revival = `git mv` back to `docs/plans/`, re-verify target spans against the current tree (a drift check is mandatory after any park), and re-run one blind review round before execution.

Deferral rationale lives on each plan's backlog origin (marked "Priority: deferred" in `docs/history/backlog/`) — read that before reviving anything here.
