# create-design-rfc (redirect)

**Canonical workflow:** [`agents/skills/rfc-design/SKILL.md`](../agents/skills/rfc-design/SKILL.md)

This command file is retained for OpenCode registration compatibility only. Do not duplicate skill content here.

Invoke the **`rfc-design`** skill when the user asks to create, draft, edit, or structurally review a Design RFC in Markdown.

- **Slash / command alias:** `/create-design-rfc` or `/rfc-design` → load `rfc-design` skill
- **Confluence review:** use `review-confluence-doc`, not this skill
- **Implementation planning after RFC:** use `plans` skill
